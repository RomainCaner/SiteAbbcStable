# Dépendances du script de récupération des classements FFBB.
# Utilisées uniquement par le workflow GitHub Actions, jamais par le site.
#
# ffbb-data-client : SDK de l'API FFBB (classements, poules, engagements). C'est
# la voie principale. requests et beautifulsoup4 servent aux équipes désignées
# par une URL de page plutôt que par un identifiant de poule.
requests>=2.31
beautifulsoup4>=4.12
ffbb-data-client>=2.4.6
